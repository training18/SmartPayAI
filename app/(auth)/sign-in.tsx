/**
 * Sign-in route — thin wrapper exposing the auth role-picker screen to the
 * router. All UI / business logic lives in `src/screens/auth`.
 */

export { default } from '@/src/screens/auth/UserTypeLoginScreen';
